@page
@model WebAdmin.Pages.Artist.DeleteModel
@{
}
<h1>Delete</h1>

<h3>Bạn chắc chắn xóa thông tin?</h3>
<div class="delete">
    <h3>Bạn chắc chắn xóa thông tin?</h3>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Tên  Tác Giả
            @* @Html.DisplayNameFor(model => model.Artist.ArtistName) *@
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Artist.ArtistName)
        </dd>
        <dt class="col-sm-2">
            Hình Ảnh
            @* @Html.DisplayNameFor(model => model.Artist.Image) *@
        </dt>
        <dd class="col-sm-10">
            <img class="@Html.DisplayFor(model => model.Artist.Image)" alt="N/A"/>
            
        </dd>
    </dl>

    <form method="post" asp-page="./Delete" asp-route-id="@Model.Artist.ArtistId">
        <input type="hidden" asp-for="Artist.ArtistId" />
        <div class="form-group">
            <input type="submit" value="Xóa" class="btn btn-primary" />
        </div>
    </form>
</div>
